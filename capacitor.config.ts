import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'event-listing-description-booking-with-history',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
